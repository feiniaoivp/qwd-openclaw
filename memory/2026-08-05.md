# 2026-08-05

## 双策略扫描 + 调参对照 归档整理 (21:13)
- 现状确认: 每日双扫描日报已存在 `analysis/daily/YYYY-MM-DD_dual.md`(如 2026-08-05_dual.md)；weekly cron `cc0ae9f1` 的 payload 步骤6+7 已调用 `param_signal_diff.py` 并推送「⚖️ 调参对照信号差异榜」——即该项**本已纳入每周cron**。
- 本次补强: 给 weekly cron 步骤6增加「将完整差异榜归档为 analysis/daily/$(date +%F)_param_signal_diff.md」，实现可回查持久化。
- 手动首份留档: `analysis/daily/2026-08-05_param_signal_diff.md`（今日跑通结果：反转0/新卖0/新买3/策略切换11/无差异15；新买=雷科防务/科华数据/巨化股份）。
- 教训: 
  - `adaptive_params_prev.json` 当前缺省 → param_signal_diff 回退 validation+默认 作为旧基线。下次 weekly cron 步骤2 会先备份 prev，形成真实「调前 vs 调后」连续对照。
  - param_signal_diff.py stdout 混入 baostock 噪音行(`login success!/logout success!/[Errno 9] Bad file descriptor 接收数据异常`)，归档时需用 awk 过滤（跳过 `PARAM_SIGNAL_DIFF`..`END` 之间 + login/logout/Errno/接收数据异常 行）。
