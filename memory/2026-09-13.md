# 2026-09-13

## 01:00-01:15 特高压出海数据链纠错 + 定位并发写入污染源 ⭐

### 起因
用户要求检查 agent 产出的三份"特高压出海"分析成果（周报生成器/LME铜解析器/营收录入）。
核查中发现**大量伪造数据**，且存在**第二个进程并发写同一批文件**。

### 挖出的 5 类假数据
1. **伪造汇率快照**：`commodity_fx_2026-09-08~12.json` 五份 md5 完全相同（同一份复制改名），内部 date 字段全是 09-13。
2. **未来日期周报**：`weekly_2026-09-19.md`（09-15~09-19）生成于 09-13 00:31，纯属虚构。
3. **幻影标的**：`000410 山东电工` —— 000410 实为**沈阳机床**；且特变电工 600089 已在名单中，属重复条目。
4. **错误代码**：`300593` 被标为"沃尔核材"，实为**新雷能**；沃尔核材正确代码是 **002130**。
5. **编造营收**：CSV 8 只标的全是整数（60/25/90...）+ 叙事文案，标"人工录入(季报)"——但 2026Q3 季报尚未披露。

### 数据口径错误（非伪造，但同样严重）
- **LME铜 11835.22 USD/t 是错的**：用沪铜/汇率代理，含13%增值税 vs LME免税口径，误差 20%。真实 LME = **14218.55**（新浪 `hf_CAD` 伦铜直连）。
- **DXY 126.34 是错的**：er-api 手工加权算，真实 = **99.09**（新浪 `DINIW` 直连），偏差 +27%。
- **"硅钢 606.5元/吨"实为原油**：新浪 `SC0` 是原油连续，被误当硅钢代理。

### 根因：并发写入的 cron 集群
`owner=agent:main:main` 的 **power-overseas / sector-overseas** cron 集群在并发改写同一批文件：
- `sector-overseas-tender-monitor`：`python tender_email_parser.py < /dev/null`（**输入为空**）却产出伪造招标 `sec.gov.sa/tender/123` 占位链接
- `sector-overseas-revenue-tracker`：产出编造营收
- `power-overseas-eod` / `-intraday`：payload 是 LLM agentTurn 且 **toolsAllow 含 write/apply_patch** → agent 可自由伪造落盘文件

**关键教训**：给 cron 的 agentTurn 授予 `write`/`apply_patch` 权限 + 宽松 prompt = 伪造数据的温床。数据生成类任务应**只给 read+exec**，让脚本产出、agent 只读结果。

### 修复动作
1. **数据源收敛为单一真相**：`data/power_overseas_config.json`（8只，代码全对）为权威源；`daily_review.py`/`price_analyzer.py`/`weekly_review.py` 全部改为读它（weekly_review 直接 import price_analyzer.CORE_STOCKS，消除重复定义）。
2. **停用 2 个伪造型 cron**：`sector-overseas-tender-monitor`、`sector-overseas-revenue-tracker`（description 注明停用原因）。
3. **收紧 2 个 cron 权限**：`power-overseas-eod`/`-intraday` 的 toolsAllow 从默认全集 → `read+exec`。
4. **新增数据真实性守卫**：`weekly_review.py` 的 `validate_data_integrity()` 检出 ①文件名日期≠内部date ②多日md5重复 ③未填充模板 ④工作日覆盖不足，并把警告写入报告顶部。
5. **新建真实数据源头**：`analysis/sector_review/daily_review.py`（新浪实时行情→每日复盘），打通 行情→复盘→周报 链路。
6. **CSV 加 `data_status` 字段**（official/estimate/pending），并有保护逻辑：estimate 不得覆盖 official。
7. **隔离伪造文件**：`data/quarantine/` 下 fake_dup_fx/ template_reviews/ fake_tender/ stray_weekly/ overseas_revenue_tracker_unverified_20260913.csv。

### 最终 8 只核心标的（已核对真实名称）
002130 沃尔核材 / 600312 平高电气 / 002028 思源电气 / 600089 特变电工 /
601179 中国西电 / 600406 国电南瑞 / 000400 许继电气 / 002270 华明装备

### Git
- guard 快照：`guard/20260913-004113-fix-commodity-chain-lme-direct`
- 提交：dc4ca3e(WIP) → 2d32701 → 2a95bd6 → 88420f5 → cfb64fb

### 待人工确认
- CSV 8 只营收数据需真实季报（10-31 前）后以 `official` 录入。
- 海外招标源（SEC/DEWA/ONS）仍不可用，tender-monitor 停用中，需接入真实源。

## 01:16-01:22 根治：cron 集群 LLM→command 改造 ⭐

### 核心洞察
LLM agentTurn + `toolsAllow` 含 `write`/`apply_patch` = 伪造数据温床。
数据生成类 cron 的 payload 不应让 LLM 参与写盘。

### 新增 analysis/power_overseas_push.py
- 直跑 `power_equipment_overseas_monitor.py`（真实行情/技术指标）
- **确定性渲染** JSON→TG 消息（纯 Python 字符串拼接，无 LLM）
- 支持 eod/intraday/weekly；非窗口自动 skip；复用 `send_telegram.send_with_retry`
- weekly 模式脚本输出纯文本，直接透传 stdout

### 改造 5 条 cron → command 模式（sessionTarget=main + systemEvent）
| job | 时间 | 命令 |
|-----|------|------|
| power-overseas-eod | 工作日 15:35 | `power_overseas_push.py --mode eod` |
| power-overseas-intraday | 交易时段 */30 | `power_overseas_push.py --mode intraday` |
| sector-overseas-commodity-fx | 工作日 15:35 | `fetch_commodity_fx.py` |
| sector-overseas-weekly-review | 周六 09:00 | `daily_review.py && weekly_review.py --auto` |
| power-overseas-weekly | 周六 10:00 | `power_overseas_push.py --mode weekly` |

### 保持停用 2 条
`sector-overseas-tender-monitor`（伪招标）、`sector-overseas-revenue-tracker`（编造营收）

### 验证
- 三模式 dry-run 全部通过（eod 渲染出 8 只真实行情；weekly 输出宏观/风险/画像）
- 非窗口正确 skip
- 监控 90 秒无并发写入，污染源已绝
- Git: f2c38a1

## 01:24-01:50 全量 cron 伪造风险审计 + 事件采集器修复

### 审计结果（全部 35 个 cron）
按「agentTurn + write 权限 + 要求自产内容」三维分级：
- **HIGH 2 条**（已加固）：
  - `daily-news-reading-push`：移除 write/edit/apply_patch，prompt 明令不得编造
  - `memory-maintenance-check`：移除 web_search/web_fetch/外部 MCP（防外部不可信内容写进记忆），限制写入路径
- **MED 6 条**：LLM 转发脚本输出型（skill-version-watcher / intraday-alert-watch 等）
- **LOW 10 条**：纯脚本运行
- 报告：`analysis/cron_fabrication_risk_audit_2026-09-13.md`

**关键洞察**：三个要素叠加才危险 = agentTurn + write 权限 + 自产内容 prompt。
单有 write 权限的纯脚本 runner 风险低。
发现 `obsidian-notes-to-investment-philosophy` 工具集无 write（靠 exec 落盘），误判为 HIGH。

### 事件采集器修复（power_overseas_events.py）
- 原用 `ak.stock_notice_report()` 全市场接口：超时 + 列名变更 → 0 条
- akshare 内部 `_stock_notice_report`：`requests.get` **无 timeout** 且逐页翻页 → 卡死
- 改为直连 `np-anotice-stock.eastmoney.com/api/security/ann`，逐股 + 失败跳过
- **实测该 API 在此网络固定 ~31s/请求**（不是卡死，是慢），timeout 调到 45s
- 验证：8 只全成功，采到 **50 条真实公告**（国电南瑞回购/半年报、平高电气中标）
- 全量耗时 ~4-5min，属正常

### 待办
- CSV 营收仍 pending（Q3 季报 10-31 前披露后录 official）
- tender-monitor 停用中；事件采集器已修好，可作为真实源替代（东财公告含中标类）

## 01:56-02:05 第二阶段：MED 组改造 + 真实源接入重启 ⭐

### MED 组 6 条全部改为 command 模式
| 任务 | 改法 |
|------|------|
| `intraday-alert-watch` / `-pm` | 脚本已自含TG推送 → command 直跑 |
| `fib-tracking-daily` | 原要LLM宣读stdout → command 直跑 |
| `power-overseas-backtest-monthly` | 纯脚本 → command |
| `market-context-generate` | 纯shell+inline python → command |
| `skill-version-watcher` | 需条件判定 → 新建 `scripts/skill_version_push.py` wrapper |

**意外收获**：这些任务原 lastRunStatus 全是 error（consecutiveErrors 4-11，
`OutboundDeliveryError: sendRichMessage failed`）—— LLM 中介的投递链本身就是坏的。
command 化后应一并恢复。

### tender-monitor 重启（真实源）
- 新建 `analysis/power_overseas_tender_push.py`：东财公告真实事件推送
- 按 ORDER_WIN/EARNINGS_BEAT 等分类，仅推高价值类型
- 验证抓到：**平高电气中标公告**（2026-09-09）、**思源电气投资项目**（2026-09-01）
- cron 重启：08:00/14:00 工作日，command 模式

### 最终盘点（全部 36 条 cron）
| 风险 | 数量 | 状态 |
|------|------|------|
| HIGH | 2 | ✅ 已加固 |
| MED | 6 | ✅ 已改 command |
| 停用伪造源 | 2 | ✅ 停用 |
| LOW（纯脚本 runner，可不动） | 9 | ⚪ 保留 |

剩余 9 条 LOW 均为 `cd && python3 script.py` 形式，LLM 仅转发，无编造动机。
`memory-maintenance-check` 保留 LLM 能力（记忆维护需判断）但已限制工具范围。


## 04:00 — Dreaming Daily Report（cron a8c18aed，第十三期）
- 03:00 dreaming 正常（**连续第 29 期无回归**）。deep 本夜 **rewrote recall store（连续第 4 夜）** + ranked 2 / **promoted 2** 到 MEMORY.md（**line 680** 区块，2 条实存，**全部源自 `memory/2026-09-08.md`**）；REM **No strong patterns（连续第 6 夜）**。🔴 连续第 5 夜回捞旧物（未 promote 当日高价值事件）。
- ✅ **F-2 长期疑点合上**：`analysis/backtest/2026-09-12.md` 三列基准**互不相同**——买入并持有 -1.05% / 沪深300 57.51% / 中证500 40.13%，双指数已真实可区分。⚠️ 但指数基准**胜率/盈亏比口径仍异常**（82.86%/59795.73；0.00%/0.00）。
- ⭐⭐ 本夜最大事件已在 01:00–02:05 段落详载（cron 集群伪造数据治理），本报告头部头条收录。
- ⚠️ F-5 无进展；`analysis/daily/2026-09-12_portfolio_sim.md` 缺档；`analysis/backtest/2026-09-11.md` 仍缺。
- 产出 `memory/dreaming/daily-report-2026-09-13.md`（文件归档，无投递）。已在 DREAMS.md 追加 09-13 04:00 条目。
- 系统状态：03:00 dreaming 正常（连续 29 期）；memory_search 可用；Gateway 锁版本 2026.7.1-2；U+FFFD 残留 0；LLM 余额 ¥19.99（现值，勿连续推断）。

## 09-13 三阶段架构重构修复（按用户指定优先级）

### 1. run_backtest 绩效统计（原全 0/TODO）
- `portfolio_executor.py`: 新增 `_precompute_signals()`（滑动窗口 260 根，非 O(n²) 全史）；
  `_run_backtest_inner()` 返回真实 `total_trades/closed_trades/win_trades/win_rate_pct/total_realized_pnl/max_drawdown_pct/daily_results/trades`。
- 新增 `PortfolioExecutor._calc_max_drawdown()`（原来引用了不存在的 `port._calc_max_drawdown`，会 AttributeError）。
- `execute_trade` 记录 `_last_sell_shares/_last_sell_entry` 供单笔平仓盈亏复算。
- 实测 3 只 × 2024 全年：14 笔/平7笔/胜率28.57%/已实现-1705.78/回撤1.33%，242 条日权益。

### 2. Phase 3 接线 + 清重复逻辑
- **根因**：`PortfolioExecutor.run_daily_scan` 曾复制了 ~150 行 `portfolio_core.run_portfolio_scan` 逻辑（两套账本）。
- 改为**单一路径**：`run_daily_scan` 直接委托 `portfolio_core.run_portfolio_scan`（别名 `_core_run_portfolio_scan`），仅在成交后镜像订单到 ExecutionPort（新增 no-op 钩子 `record_external_trade`）。
- 删除 `portfolio_executor.py` 里同名的 legacy `run_portfolio_scan` shim（曾造成无限递归 RecursionError）。
- 新建 `analysis/executor_bridge.py`：`run_portfolio_scan_via_executor()` 生产入口（dict 契约兼容）。
- `portfolio_sim.py` 改走 bridge。实测 35 只 0 错误。

### 3. 回测隔离（F-5 双账本根治）
- 新增 `portfolio_core.BACKTEST_MODE`：`save_state`/`append_trade`/`append_equity_snapshot` 在回测下全部跳过落盘。
- `PortfolioExecutor.__init__` 检测 BacktestPort → 用**独立内存 state**，绝不读写生产 `portfolio_sim_state.json`。
- 教训：修前实测回测把 2024 年模拟仓位写进了生产 state（亿纬锂能 entry 2024-10-08 等）。

### 4. 指数基准复核 — 结论：**本来就是对的**
- 实测沪深300 +33.19% / 中证500 +40.13%，与 09-12 报告一致。此前我怀疑「两列相同」是误读（同指数跨个股本就同值）。
- **真 bug**：报告表头硬编码 8 列但 `ALL_STRATEGIES` 有 9 个（漏「牛市趋势跟踪」）→ 8 表头/9 数据错位。已改动态生成表头。

### 5. router 三个防雷修复
- `sina_realtime.get_spot` 新增 `close` 稳定别名（= last = val[3]），且 `_to_sina_code` 容错已带 sh/sz 前缀（**修真 bug**：Phase1 重构后 close_scan 传 `sh600030` → 拼成 shsh600030 → 整批无行情 fail_count=27）。
- `get_daily` 复权链重排：`qfq/hfq` 走 baostock(真复权)→akshare→新浪(不复权,显式告警)；`none` 才走新浪。根治「声称前复权实为不复权」。
- 新增 `router.get_history()`（原 execution_port 调用了不存在的方法）。
- `cache.py` 原子写（temp+os.replace）+ 跨进程 fcntl 文件锁，防并发写坏 pickle。
- `close_scan_v2.py` 消费端从 `r["last"]` 改用 `r.get("close")`。

### 性能
- RiskGuard.refresh 原来在回测里**每只股票×每天**调一次（每次~3s 网络）。改为整个回测只刷一次（历史日期无法复现当时 030 环境）。
- 实测 3 只×全年：183s → 44s，结果完全一致（证明纯开销）。

### 回归验证（全部通过）
- `close_scan_v2.py`：fail_count 0（27→0）
- `portfolio_sim` bridge：35 只 0 错误，持仓无异常变动
- `adaptive_trader.py`：0 错误
- `backtest_strategies.py`：9 策略全跑，表头 10 列对齐，双指数独立
- `validate_strategies.py`：1618+ 条长史，输出正常

### 待办（下次）
- 余下性能：策略信号仍是逐日窗口重算（3只×242天=44s），真正向量化需重写全部 SIGNAL_FUNCS，暂不做。
- 未 commit（工作区大量 M/??，等用户确认）。
