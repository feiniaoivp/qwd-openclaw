## 2026-08-08 梦境/记忆巩固

### LanceDB Memory Dreaming cron 补跑（04:55 手动触发）
- **背景:** cron `e435cf03`（LanceDB Memory Dreaming，03:00 每日）最近一次运行 `lastRunStatus=error`。诊断：`Edit: in ~/.openclaw/workspace/MEMORY.md (5495 chars) failed`。
- **核查发现系统性 bug:** 梦境管线 light/REM/deep 各阶段运行正常并产出候选，但 **deep 阶段的「把候选写入 MEMORY.md」这一步反复失败**。MEMORY.md 底部的 `## Promoted From Short-Term Memory` 只到 `(2026-08-07)` 且内容仅含 `memory/2026-08-02*.md` 来源——即 08-03 起每一个 dreaming 运行的持久化都静默失败了，只留下 deep 文件里「Promoted N candidates」的虚假成功标识。
- **本次完成:** 将 08-07 梦境 staged 的 5 条候选（在 `memory/dreaming/light/2026-08-08.md`，全部 status:staged，evidence 指向 `memory/2026-08-07.md`）正式写入 MEMORY.md，新增区块 `## Promoted From Short-Term Memory (2026-08-08)`，含标准 `openclaw-memory-promotion` 标记：
  1. 新闻知识库语义检索系统搭建（.venv-ml / torch 2.2.2 单线程 / faiss BGE 512维）
  2. 关注股当日新闻阅读自动化（daily_news_reader + cron daily-news-reading-push）
  3. daily-a-share-telegram-push 8层根因超时修复 ⭐
  4. 其它脚本同步 akshare/baostock 数据源修复（新浪日K首选）
  5. 启用真 DeepSeek LLM 解读（agent 健康度/风险建议）⭐
- **安全:** 改前备份 `MEMORY.md.bak.20260808_045712`。MEMORY.md 338→351 行。
- **⚠️ 待跟进:** 该 edit 失败是管线固有缺陷（大块 ~5.5KB 追加到 MEMORY.md 时锚点不唯一/超限），未来 dreaming 运行可能继续失败。建议：
  - 评估是否仍需要这个 legacy「LanceDB Memory Dreaming」cron（配置里 `memory-lancedb-dreaming` 插件已 `enabled:false`，实际活跃是 `memory-core` 的 dreaming）。
  - 若保留，需要修 deep 阶段的追加逻辑（改用 append 而非需要唯一锚点的 edit）。
