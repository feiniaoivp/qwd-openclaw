# 2026-08-25 (周一)

## 盘中策略预警系统接入 close_scan_v2 (05:00-05:15)
- **需求**: 用户要求把"结合地缘/新闻的策略应对"接入 close_scan_v2 每日扫描做**盘中实时预警**。
- **做了什么**:
  1. 新建 `data/intraday_alert_config.json` — 30只关注股手动策略规则(止损位/买点区/急涨急跌/行动指示)，basis=08-24收盘+地缘分析。
  2. close_scan_v2.py 新增盘中策略预警引擎 `evaluate_strategy_alerts()` + `run_intraday_alert_mode()` + `--intraday` CLI；在主 scan 输出 JSON 加 `strategy_alerts`/`strategy_alert_triggered` 字段。per-价位去重(state 文件 `data/intraday_alert_state.json`)防重复轰炸。
  3. 新建 `scripts/push_intraday_alerts.py` — command 直跑自含脚本，调 close_scan_v2 --intraday，仅新触发时组纯文本(parse_mode='')推送 telegram；无触发静默。**绕开 LLM**(防超时/fallback崩)。
  4. 新建 cron `intraday-alert-watch`(c57f99fb) `*/30 9-15 * * 1-5` Asia/Shanghai，isolated agentTurn 跑 push 脚本。
- **验证**: 完整主 scan 正常(hist_scan, 30只)；intraday 模式触发3条(长电73.8/奔图16.75/福莱蒽特25.1 恰好停在昨收止损线)；telegram 测试推送成功。
- **⚠️ 注意**: cron add 接口 payload.kind 只收 agentTurn/systemEvent，`command` 是旧 legacy 格式(如 portfolishim-daily)无法新建 → 用 agentTurn 直跑脚本。盘中预警在 09:00-15:00 每30分钟轮询。
- **关键校准**: 长电/奔图/福莱蒽特 8-24 **收盘价恰好停在设置止损线**，属真实"临止损"预警。后续每个交易日盘中将用实时价独立评估。

## 投资理念每日归纳 cron (19:36) — 无新增
- 扫描 Obsidian Vault `/Users/duguke/Documents/Obsidian Vault`，过去24h(08-24 19:36 起) **0 篇新增/修改 .md**。
- 窗口内唯一触碰笔记《12個技術指標只有3個不可取代》(08-24 18:22) 已于 08-24 归纳处理（wiki/sources/investment-philosophy-2026-08-24.md + MEMORY.md「2026-08-24 要点」均完整，含"表面冲突底层自洽"的MACD待复核项）。
- 结论：**无新增规则、无修正、无冲突**，体系稳定。未生成新 wiki 文件（08-24 已是该笔记的归档）。
