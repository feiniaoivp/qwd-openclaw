# 2026-09-11（周五）

## 04:00 — Dreaming Daily Report（cron a8c18aed，第十一期）
- 03:00 dreaming 正常（**连续第 27 期无回归**）。deep 本夜 rewrote recall store（**连续第 2 夜**）+ ranked 3 / **promoted 3** 到 MEMORY.md（新区块 **line 662** `## Promoted From Short-Term Memory (2026-09-11)`，已核对落盘 3 条实存——来源 09-06-weekly-review 1 / 09-06 2，**连续第 3 夜回捞 09-06 旧物，未 promote 09-10 当日高价值事件**）/ REM 4 主题（weekly-review 同源分词噪声，conf 0.95 但非真实真理）——安静巩固夜。
- 📌 **头条：09-10 用户侧基建变更提交 guard 快照**——09-10 14:52 git 提交 `019b40f`（「op CLI登录、guard快照补全、F-2双指数真实数据、F-5资金口径复核」）+ guard tag `guard/20260910-145303-...`。guard 快照断档待办**部分处理**（近 30 天首次）。
- ⚠️ **「变更已提交」≠「产物已生效」**：①F-2 声称修双指数，但 09-10 回测沪深300==中证500==61.65% **未变**（买入并持有 -1.07% 真实✅）→ 查是否未重跑/缓存覆盖；②F-5 称复核但 **portfolio_sim 09-10 断档**（最近 09-09 23:11）；③op CLI「已登录」待主会话/安全审计核验。
- ✅ 09-10 交易日管道：晨盘 dual/next_day_sim/premarket 全 ok；收盘 signal_audit（19:03 **0 冲突**）+ challenge_review + param_eval（14:47，29 只/成交 5）正常。
- 产出 `memory/dreaming/daily-report-2026-09-11.md`（文件归档，无投递）。已在 DREAMS.md 追加 09-11 04:00 条目。

## [cron] 每日Obsidian→投资理念归纳 (19:30)
- 24h真实增量3篇: MACD进阶 / RSI压缩剥头皮 / 斐波那契时间预测
- 无head级新增规则、无规则级冲突; 新增修正候选⑫(MACD多周期+动能分离+裸K) ⑬(斐波时间线作变盘时点预警); RSI定位⑧连续第5日强化(与09-10回测实证张力持续)
- 归档: wiki/sources/investment-philosophy-2026-09-11.md; MEMORY.md「投资理念归纳」区块已更新(最新2026-09-11); Telegram已推送(chat_id 626141741, ok=True)

## 20:23–20:40 — 🔴 U+FFFD 编码污染根因修复（source-level，非渲染问题）
**用户反馈**：挑战质疑报告里信号标签出现乱码（`���🟡 关注`、`�招商银行`）。
**根因**：不是编码转换 bug，是**源码字面量被写坏**——`analysis/service.py` 中直接硬编码
`level = "���🟢 � 强烈�买入"`。约 08-04 起某次批量"美化"编辑（加 emoji/装饰空格）导致字节损坏，
污染固化进源码，此后每日产出均继承。波及 **199 个文件**、**199 处**。
**修复 1+2+3+4**：
1. 定位：`service.py` 为信号标签源头；`prompts/risk_instruction.txt` 是喂给 LLM 的风险规则（本身乱码）
2. 清源 7 个核心文件：service.py(142→0) / agent/nodes.py(101→0) / risk_instruction.txt(41→0) /
   trader_stock_picks.py(61→0) / llm_tool.py(28→0) / rag/experience_store.py(22→0) / state.py+graph.py(19→0)
3. 护栏：新建 `analysis/encoding_guard.py`（sanitize/is_clean/guard_write/validate_level + VALID_LEVELS 白名单），
   接入 service.py 两个信号出口 + challenge_review.py 的 load_state(递归清洗)
4. 验证：29 份历史报告清洗；analysis/ **0 残留**；重跑 challenge_review 输出干净；冒烟测试 calc_full_signal 正常
**⚠️ 重大副作用发现**：修好后报告从 **2 项质疑 → 13 项**。乱码一直在**掩盖风控矛盾**——
`"强烈�买入" in level` 这类模糊匹配在脏数据下静默失配，审核器形同虚设。
**备份**：`backups/encoding-fix-20260911/`

## 20:37 — 中联重科"卖出信号被忽略"疑点查证（规则误报，已修）
**结论：不是信号被忽略，是审计规则必然误报。**
- 事实：策略2 今日 `sell=True` 但 **`position:false` 空仓**；空仓时卖出信号无意义（=不宜买入提示），与 HOLD 不矛盾
- Bug：`check_zhonglian_vs_advice` **从不写 `holding` 字段** → `describe_zhonglian` 里
  `f.get("holding", True)` 恒为 True → **永远走"被忽略"分支**（从 08-10 起每天必报）
- 修复：新增 `_zhonglian_strat2_holding()` 从 `data/zhonglian_state.json` 读 `strategy2.position`；
  空仓时 severity 动态降级 HIGH→LOW（`_severity_override`，在 run_all_challenges 中生效）
- 验证：09-11 中联从🔴高危第4位 → 🟢低危第13位，高危 4→3（真实矛盾）；
  回归 08-19（当时**确持仓中**）规则正确保留高危 ✅ 未一刀切掩盖
**🔍 顺带发现真问题（待用户决策）**：策略2 从 **08-06 至 09-10 连续 26 个交易日 `sell=True`**——
`sell` 条件含 `RSI<60`，在 RSI 长期 19~40 的下跌趋势中恒为真 → **卖出标志卡死无法复位**。
RSI 一度跌至 **3.3**（08-19，历史级超卖）。**空仓状态下 sell 是否应复位归零，还是保留作"不宜买入"持续提示？待定。**

## 20:40 — C 方案实施：卖出信号事件化（中联重科策略2 卡死修复）✅
**用户决策**：C 方案——卖出信号应为「事件」而非「状态」。
**Bug 机制**：`sig2_sell = macd_bear or (latest["close"] < ema26.iloc[-1])`
- `macd_bear` 是**事件**（仅死叉当天 True）✅
- `close < EMA26` 是**状态**（价格在均线下方即恒 True）❌ → 下跌趋势中 sell 永久卡死
- 实测 08-06~09-10 **连续 26 个交易日 sell=True**，期间 RSI 低至 3.3
**修复**：
```python
price_broke_ema26 = (close[-1] < ema26[-1]) and (close[-2] >= ema26[-2])  # 下穿当天
sig2_sell = macd_bear or price_broke_ema26
```
新增字段 `sell_event`（"MACD死叉"/"下穿EMA26"/None）与 `below_ema26`（状态保留但不再驱动 sell）。
**⚠️ 发现同 bug 存在于 3 处（非 1 处）**，已全部同步修复：
1. `analysis/service.py:393`（agent 主链路，源头）
2. `analysis/close_scan_v2.py:841`（**活跃生产脚本**，premarket_report 每日调用）
3. `analysis/zhonglian_monitor.py:81`
**验证**：
- 2024-01-01~今日 240 交易日 → 卖出事件 **25 次**，平均间隔 12.9 天（min 1 / max 29），事件密度合理
- 边界正确：08-04 判定"下穿EMA26"（08-03 在上方 ✅）、08-05 为"MACD死叉"（非重复下穿 ✅）
- 09-10/09-11 修正后 `sell=False`（此前恒 True）
- 三脚本输出一致、语法/导入 OK
**教训**：同一策略逻辑在多脚本中重复实现（service.py / close_scan_v2.py / zhonglian_monitor.py），
改一处必须全局 grep 同款代码；建议后续抽出共享模块避免三处漂移。

## 20:43 — 重构：中联双策略抽为共享模块 `analysis/strategies.py` ✅
**动因**：同一策略逻辑在 3 个脚本重复实现（service.py / close_scan_v2.py / zhonglian_monitor.py），
导致同一「卖出信号卡死」bug 必须修三处（本次实证）。用户要求立即消除漂移隐患。
**新建 `analysis/strategies.py`**：
- `compute_indicators(df)` — MACD/EMA/RSI + 交叉事件统一计算
- `zhonglian_strategy1(ind)` / `zhonglian_strategy2(ind)` — 双策略判定
- `analyze_zhonglian(df, macd_state_style=)` — 统一入口，支持 `"default"`(📗/📕) 与 `"plain"`(多头金叉) 两种文案风格
- 常量集中：`STRATEGY1_NAME` / `STRATEGY2_NAME` / `SELL_EVENT_*`
**三个调用方改为引用共享模块**：
1. `service.py` → thin wrapper `analyze_zhonglian()`（向后兼容，外部无需改）
2. `close_scan_v2.py` → 同名函数委托，`macd_state_style="plain"` 保持原文案
3. `zhonglian_monitor.py` → `check_signals` 改用共享指标+策略函数，本地仅保留特有 desc 格式
**验证（全部通过）**：
- 等价性回归：35 天抽样，新旧实现输出 **0 处不一致**
- 三调用方交叉对比：除风格文案外完全一致
- 生产端到端：`zhonglian_monitor.py` / `close_scan_v2.py` 实跑正常，中联段输出正确
  （close_scan_v2 的 `zhonglian` 段 MACD_state="多头死叉" 风格保持 ✅）
- agent 主链路 + challenge_review 正常
- 全仓库 U+FFFD 残留 **0**
**收益**：以后改策略只动 `strategies.py` 一处，杜绝三处漂移。
