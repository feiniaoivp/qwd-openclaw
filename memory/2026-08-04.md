## 2026-08-04 双策略最优扫描 adaptive_dual.py
- 新建 `analysis/adaptive_dual.py`：每股跑 score 前二策略（从 validation_2026-08-01.json details 取按 score 降序前2），复用 adaptive_trader 的 signal 函数与数据链路(baostock+新浪补丁)。
- 输出 DUAL_SCAN_RESULT JSON + 日报 analysis/daily/YYYY-MM-DD_dual.md。
- 已接入 cron `daily-a-share-telegram-push`(3950d3cc)，收盘15:30简报新增「⚡双策略分歧榜」段（解析 consensus==分歧 的股票）。
- 已验证跑通(exit=0)：覆盖30只(缺机电B股900925 baostock无数据)，盘前时段 change_pct 显示+0.00%属正常(新浪盘前返回昨收价)。
- 策略映射: 布林带+ATR→bollinger / KDJ+CCI→kdj_cci / EMA+OBV→ema_obv / EMA12/26金叉(基准C)→ema_cross / 纯MACD(基准B)→macd。
- 双策略使用分布占比: EMA12/26 21次 > 纯MACD 12次 > 布林+ATR 11次 > KDJ 8次 > EMA+OBV 6次。
## 2026-08-04 参数自调优闭环(closed-loop)搭建完成 + 首轮全量调优
- **搭建四层闭环**（用户要求"整个过程中不断自我迭代调优参数"）：
  1. `analysis/param_tune.py`（新建）：参数网格搜索引擎，每股在5策略参数网格上于4时窗(6.5年/3年/1.5年/1年)搜索最优(策略,参数)。网格: 布林 length{15/20/25}×std{1.5/2/2.5}×atr_mult{1/1.5/2}; MACD fast{10/12/15}×slow{22/26/30}×signal{7/9/12}; EMA fast{8/10/12/15}×slow{21/26/30}; KDJ length{7/9/12}×rsi_threshold{30/40/50}。评分=夏普×60+收益×0.6-回撤惩罚)×样本量权重。护栏: 得分≥25且夏普≥阈值(低笔数需更严)。输出 PARAM_TUNE_RESULT。
  2. `data/adaptive_params.json`：存每股最优策略+参数+details。
  3. `adaptive_dual.py` 升级：从 adaptive_params.json 读每股最优参数跑前二优势策略信号（之前只有策略级固定参数）。信号函数全部参数化(signal_* 接受 params dict)。
  4. weekly cron `weekly-backtest-strategy-refresh` 扩展：单窗口回测→跨周期验证→全量参数调优→自动写回+推送。
- **首轮全量调优成功**（pid 67126，24分钟1447s，2026-08-04 05:46写入）：30只全部拿到最优参数写回 adaptive_params.json，仅机电B股900925数据不足(baostock无数据，已知)。策略/参数大量变更，例如: 久立特材bollinger→ema_cross(8/30)、亿纬bollinger(15/1.5/1.0)、招商macd→kdj_cci(12/50)、国瓷macd(15/26/12)得分343夏普2.245。
- **调参前后信号对比**（29只有效，20只信号/策略变化）：
  - 调参后多只由"持有"变成明确卖出信号：中信证券bollinger卖出、中联重科bollinger卖出、恒立液压bollinger卖出、中芯国际kdj卖出、福莱特kdj卖出。
  - 巨化股份调参前macd金叉买入，调参后改为ema_cross作为第一策略+macd均持有（策略降级）。
  - 通富/天齐/科华旧主策略ema_obv卖出信号，调参后主策略转ema_cross/macd持有（EMA+OBV不再是历史最优）。
- **重要认识**: 参数级调优(非仅策略选择)能显著提升单股历史表现(国瓷得分272→344, 夏普2.06→2.25)。但历史最优≠未来，护栏防过拟合是必要的。
- 遗留: 收盘简报用的 adaptive_dual 在盘后运行，用的参数文件需确保是当日最新调优结果。14:05核对cron已设(66e01efa)后确认完成。
## 2026-08-04 调参对照信号差异榜 纳入每周cron
- 新建 `analysis/param_signal_diff.py`：对比"调前参数"vs"调后参数"的每股双策略信号，生成差异榜(PARAM_SIGNAL_DIFF JSON)。归类: 🔁方向反转(最重要)/🔴新卖出/🟢新买入/🔄策略切换但持有。旧参数源: adaptive_params_prev.json, 缺失回退 validation+默认参数基线。
- 更新 weekly cron `weekly-backtest-strategy-refresh`(cc0ae9f1)：调参前先 `cp adaptive_params.json adaptive_params_prev.json` 备份基准 → backtest → validate → param_tune --write → param_signal_diff 生成差异榜 → 推送Telegram含⚖️调参对照信号差异榜(反转/新卖/新买/策略切换)。
- 验证: param_signal_diff.py 跑通(exit=0)，基线用validation默认参数对比本轮全量参数: 反转0/新卖3(中信证券·中联重科·恒立液压 bollinger新卖出)/新买0/策略切换11/无差异15。
- 已删除临时核对cron 66e01efa。

## 记忆维护 (05:20 cron memory-maintenance-check) ✅
- 阅读 07-31 / 08-02 / 08-03 / 08-04 日志并蒸馏更新 MEMORY.md
- 新增归档: 新浪日K接口纳入主链路 / trader-stock-picks实战+候选盯盘 / 08-03审计guard tag=0异常 / 08-04 双策略扫描adaptive_dual + 参数自调优闭环(param_tune/adaptive_params/param_signal_diff) / 量化全链路文件索引
- Lessons新增2条: 新浪日K兜底+pytdx不接入 / 天量高开低走=出货非强势
- 错误注入: 运行 error-inject --min-count 2，4个错误模式均1x低于阈值2，无注入(正常)
