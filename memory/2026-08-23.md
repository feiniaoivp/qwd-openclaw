# 2026-08-23（周日）

## 05:41 继续 08-22 每周回测 command cron 收尾（修复 param_signal_diff 导入 bug）
- **背景 baton**：08-22 22:34 新建 command cron `weekly-backtest-pipeline`(c4cb94a5) 并 force 验跑，baton 注明「跑通后停用旧 agentTurn cron cc0ae9f1 防周六双跑」。
- **发现**：force-run 昨日 23:38 结束，`all_success=False`，8 步中仅 **`param_signal_diff` 失败**（`command exited with code 1`）：
  - 报错 `ModuleNotFoundError: No module named 'analysis'`，来源 `param_signal_diff.py` → `import adaptive_dual` → `from analysis.moat_factor import ...`。
  - **根因**：`param_signal_diff.py` 顶部队列只把 `analysis/` 插进 sys.path，**没插 `WORKSPACE` 根** → `analysis` 顶层包不可导入。手动跑（`PYTHONPATH=workspace`）参数在父进程提供根路径所以正常；pipeline 的 `run_script`（cwd=WORKSPACE 但子进程不继承 PYTHONPATH）时崩。这正好是 08-22 日志里已记录的手动补跑所需 workaround 的自动版根因。
- **修复**：`param_signal_diff.py` 顶部 `sys.path.insert(0, WORKSPACE)`（置于 analysis 插入之前），让 `analysis.xxx` 顶层包自身可导入。已验证：`/usr/bin/env -u PYTHONPATH python3 analysis/param_signal_diff.py` 退出码 0，正常输出差异榜（本轮摘要：反转0/新卖0/新买0/策略切换1）。
- **除 param_signal_diff 外**，force-run 其余 7 步全成功，简报已推送 TG msg 1355（差异榜当时因此落空）；本轮修复后将用后台完整重跑确认 all_success=True 并重推正确简报。
- **待办（重跑确认全绿后）**：停用旧 agentTurn cron `weekly-backtest-strategy-refresh`(cc0ae9f1)，避免周六 06:00 与新 command cron c4cb94a5 双跑。若本次确认失败则暂不停用、继续查。
