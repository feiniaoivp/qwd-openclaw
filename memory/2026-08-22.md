# 2026-08-22（周六）

## 04:00 Dreaming Daily Report (a8c18aed) ✅
- 03:00 dreaming pipeline 正常完成（**连续 15 期无回归**）：light 多条候选 staged / **deep 晋升 7 条到 MEMORY.md**（新区块 line 389） / REM「无强模式」（安静期延续，连续 4 期）。
- 本报告产出 `memory/dreaming/daily-report-2026-08-22.md`（文件归档，无投递），并在 `DREAMS.md` 追加 08-22 04:00 日记条目 + Deep Sleep 摘要。
- 本期核心素材（08-21）：
  1. **每日量化回测**：纯MACD 综合最优（总分 29.2，平均收益 +51.46%），推送 TG message 1350。
  2. **🔴 send_telegram.py token 失效(401)**：内置 BOT_TOKEN 过期，credentials/telegram_bot_qwd.token 有效，待改读 credentials 文件。
  3. **Obsidian 归纳无新增**：ResonanceGate 正常拦截 6 项高危质疑；13 项历史重复 BUY（招商/福耀/中信金属/恒力石化）待人工复核钝化。
- ⚠️ **weekly-backtest 验证点今日 06:00** 已到（08-17 锁 deepseek 后首个真正验证点）。
- memory_search 本报告生成时正常（bge-m3 未超时）。

## 05:51 send_telegram.py 令牌重构 ✅（修复梦中 04:00 记录的第2项待办）
- **问题**：`send_telegram.py` 硬编码的 BOT_TOKEN（`8782173086:AAH8AtjSokEhvvqc9DV-KmNnLFbj6MPUrmE`）已失效（401），本次改用 `credentials/telegram_bot_qwd.token` 中的有效令牌才成功。
- **修复**：脚本改为 `_load_bot_token()` 从 `credentials/telegram_bot_qwd.token`（明文一行）读取令牌；文件缺失/为空时回退旧硬编码值并打印警告，仅作兜底。
- **验证**：发送测试消息 `ok:true`，以 `qwd1_bot` 名义送达（TG message 1352）。注意：消息含 `.token` 等点号后缀会被 Telegram Markdown 当成实体标记触发 400，带文件名需用纯文本或转义。
- **教训**：脚本凭据一律从文件读取，不要硬编码进代码；令牌失效时应先查 `credentials/` 目录。

## 22:10 每周回测今日 06:00 失败诊断 + 人工补跑 ✅（验证点结论：不合格）
- **结论**：weekly-backtest-strategy-refresh (cc0ae9f1) 今日 06:00 失败，`lastRunStatus=error`，`lastRunError: ⚠️ 🧰 Process: crisp-nudibranch failed`。**08-17 锁 deepseek 后的验证点不合格**——但这周失败点更靠后：**数据分析全部完成，死在最后 LLM 简报生成+推送环节**。
- **证据链**：06:10 回测报告、06:16 validation、06:56 adaptive_params 全部写盘成功；仅 param_signal_diff 未产出、最终简报未推送。崩点在 06:57 后（`crisp-nudibranch` 子 agent/进程负责 LLM 简报）。
- **模型画像**：近几周失败的共同点都是**落到 nvidia fallback**（nemotron-3-super-120b），回退模型在长流程里不稳定（08-15 model-call-started 2400s、08-22 子进程崩）。主模型 deepseek 跑通的 08-08 正常。
- **人工补跑**：`PYTHONPATH=workspace python3 analysis/param_signal_diff.py` 补出差异榜（需从 workspace 根跑，否则 analysis.moat_factor 相对 import 失败 + 用 exec timeout 而非 mac timeout 命令）。简报用纯文本 parse_mode 推送成功（TG msg 1354）。
- **⚠️ send_telegram.py 新坑**：默认 `parse_mode=Markdown`，含 `()`、`/`、`300285(` 等会触发 Telegram 实体解析 400（can't parse entities）。含技术符号的简报须用 `parse_mode=''` 纯文本发送。
- **signal_diff 摘要**：反转0 / 新卖1(奔图科技 002180 EMA+OBV卖出) / 新买0 / 策略切换5(中联/科华/恒立/金力/中船汉光) / 跳过18（一侧无策略，数据缺失）。
- **param_eval 恶化 2 只待回滚**：国瓷材料 300285（-4.90%）、恒生电子 600570（-4.79%），建议结合行情回滚到 adaptive_params_prev.json 版本。
- **下一验证点**：下周六 08-29 06:00（nextRunAtMs 1787954400000）。建议：若再落 nvidia fallback 崩，需查 weekly_full_pipeline.py 是否应改 command cron（脚本本身自含全步骤，文档里也标注『建议作为 command cron 运行』避开 LLM 超时）。

## 22:34-23:00 第1项(回滚判断)+第2项(根治超时)执行 ✅

### 第1项：300285/600570 是否回滚/减仓 —— 结论：**不需要回滚/减仓**
- 按用户标准（看模拟盘实际用的策略近两周表现）验证：模拟盘用的是 `adaptive_strategy_map.json` 的策略名+默认参数（**portfolio_sim.py 不读 adaptive_params.json 的 stocks**，只读 strategy_map 策略名 + SIGNAL_FUNCS 默认参数）。
- **国瓷 300285(strategy=macd)**：08-19 从 74.26→64.12(-13.65% 单日大跌)，模拟盘卖在 64.12(-4.90%)；卖后继续跌到 62.92，08-21 反弹 67.86。卖后至今 -8.62% → **止损逃顶正确**。
- **恒生 600570(strategy=bollinger)**：08-14 卖在 22.12(-4.79%)；卖后一路阴跌至今 -4.91% → **止损避开下跌正确**。
- **关键洞察**：param_eval 判"恶化"是已实现亏损统计，但这是**保护性止损的胜利**（死扛会更惨），非策略失效。两只当前策略均"持有/空仓/无信号"，纪律健康。**不执行回滚，不改策略。** 另：portfolio_sim 用默认参数而非 adaptive_params 自定义参数，故"回滚参数"对模拟盘本身无意义（那只影响盘前分析）。

### 第2项：根治每周回测超时 —— 新建 command cron ✅
- **根因确认**：旧 cron 是 agentTurn（isolated agent 调 deepseek/流水线），LLM 环节反复超时/崩（consecutiveErrors=3）。weekly_full_pipeline.py 本身自含全 7 步脚本编排，文档明确建议作 command cron 跑。
- **增强 pipeline**：加步骤8 build_brief()+push_brief()——读取各步骤产物生成纯文本简报，用 requests parse_mode='' 推送到 TG（不经 LLM，绕开 Markdown 实体 bug）。已语法验证 + 简报生成质量良好（含汇总/变更/差异榜/赛后验证段）。注意 param_signal_diff.py **不写文件只打 stdout**，简报差异榜靠 pipeline 捕获其 stdout。
- **新建 command cron**：`weekly-backtest-pipeline`（id c4cb94a5-1b61-43cc-a870-0a96d24f6a6d），cron `0 6 * * 6` Asia/Shanghai，`payload.kind=command` sh -lc `cd workspace && python3 analysis/weekly_full_pipeline.py`，cwd 指定，timeout 7200s，announce telegram 626141741。与 portfolio-sim-daily 同款（已验证稳定）。
- **force 验证中**：已 cron run force 触发，进程确认在跑（backtest_strategies.py 已启动），预计 40-60 分钟跑完 8 步，结束后自动推送简报。**跑通后应停用旧 agentTurn cron (cc0ae9f1)，避免周六 06:00 双跑。**
- **教训**：`portfolio-sim-daily` 是 command cron 的可信模板——解决定时任务超时，优先判断是否能用 command 直跑脚本绕过 LLM。
